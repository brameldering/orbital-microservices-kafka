apiVersion: skaffold/v4beta6
kind: Config
build:
  artifacts:
    - image: orbitelco/seed
      context: seeder-service
      sync:
        manual:
          - src: src/**/*.ts
            dest: .
      docker:
        dockerfile: Dockerfile
  local:
    push: false
manifests:
  rawYaml:
    - ./infra/k8s/set-1/*
deploy:
  kubectl: {}
